import * as React from 'react';
import { AppPage, PageContainer, PageHeader } from '@/components/app/patterns.js';
import { Button } from '@/components/heroui/button.js';
import { Card,CardContent,CardDescription,CardHeader,CardTitle } from '@/components/heroui/card.js';
export function AgentOnboarding({connections,onChooseMastra,onChooseConnection,busy=false}:{connections:readonly {id:string;displayName:string}[];onChooseMastra:()=>void;onChooseConnection:(id:string)=>void;busy?:boolean}){
 return <AppPage aria-label="Mailbox Manager onboarding"><PageContainer measure="form" className="grid gap-4"><PageHeader title="Choose your Mailbox Manager" description="Choose Hypermail AI or connect Hermes. A Manager is required during setup; you can select None later for an individual Mailbox." /><Card><CardHeader><CardTitle>Hypermail AI</CardTitle><CardDescription>Use Hypermail’s built-in Manager.</CardDescription></CardHeader><CardContent><Button disabled={busy} onClick={onChooseMastra}>Use Hypermail AI</Button></CardContent></Card><Card><CardHeader><CardTitle>Connect Hermes</CardTitle><CardDescription>Authorize an existing verified Hermes Agent Connection. Connecting does not grant access to any Mailbox.</CardDescription></CardHeader><CardContent className="grid gap-2">{connections.length?connections.map(c=><Button key={c.id} variant="outline" disabled={busy} onClick={()=>{ onChooseConnection(c.id); }}>Choose {c.displayName}</Button>):<p role="status" className="text-sm text-muted-foreground">No verified Hermes connection is available. Connection setup is unavailable in this deployment.</p>}</CardContent></Card></PageContainer></AppPage>;
}
