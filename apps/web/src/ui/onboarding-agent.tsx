import * as React from 'react';
import { Button } from '@/components/heroui/button.js';
import { Card,CardContent,CardDescription,CardHeader,CardTitle } from '@/components/heroui/card.js';
export function AgentOnboarding({connections,onChooseMastra,onChooseConnection,busy=false}:{connections:readonly {id:string;displayName:string}[];onChooseMastra:()=>void;onChooseConnection:(id:string)=>void;busy?:boolean}){
 return <section aria-labelledby="manager-onboarding-title" className="mx-auto grid min-w-0 max-w-xl gap-4 p-4"><header><h1 id="manager-onboarding-title" className="text-2xl font-semibold">Choose your Mailbox Manager</h1><p className="text-muted-foreground">Choose Mastra or connect Hermes. A Manager is required during setup; you can select None later for an individual Mailbox.</p></header><Card><CardHeader><CardTitle>Mastra</CardTitle><CardDescription>Use Hypermail’s built-in Manager.</CardDescription></CardHeader><CardContent><Button disabled={busy} onClick={onChooseMastra}>Use Mastra</Button></CardContent></Card><Card><CardHeader><CardTitle>Connect Hermes</CardTitle><CardDescription>Authorize an existing verified Hermes Agent Connection. Connecting does not grant access to any Mailbox.</CardDescription></CardHeader><CardContent className="grid gap-2">{connections.length?connections.map(c=><Button key={c.id} variant="outline" disabled={busy} onClick={()=>{ onChooseConnection(c.id); }}>Choose {c.displayName}</Button>):<p role="status" className="text-sm text-muted-foreground">No verified Hermes connection is available. Connection setup is unavailable in this deployment.</p>}</CardContent></Card></section>;
}
