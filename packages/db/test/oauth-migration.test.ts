import { readFileSync } from 'node:fs';import { fileURLToPath } from 'node:url';import { describe,expect,it } from 'vitest';
const sql=readFileSync(fileURLToPath(new URL('../drizzle/0006_agent_oauth.sql',import.meta.url)),'utf8');
describe('OAuth migration',()=>{it('stores only digests and tenant identities',()=>{expect(sql).toContain('"code_digest" text PRIMARY KEY');expect(sql).toContain('"token_digest" text PRIMARY KEY');expect(sql).not.toMatch(/"(code|access_token|refresh_token)"\s+text/i);expect(sql).toContain('oauth_codes_owned_mailbox');expect(sql).toContain('oauth_public_clients_owned_connection');});it('stores revision snapshots',()=>{for(const value of ['lifecycle_revision','assignment_revision','grant_revision','safety_revision','consumed_at','family_id'])expect(sql).toContain(value);});});
